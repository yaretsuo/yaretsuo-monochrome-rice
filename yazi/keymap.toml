[manager]
prepend_keymap = [
    { on = [ "D" ],       run = "remove --permanently --force", desc = "Delete forever" },
    { on = [ "<Enter>" ], run = "open",                         desc = "Open the selected files" }
]
